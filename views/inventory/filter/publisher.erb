<div class="header">


<h1 class="header_text">Inventory Filtered by: <%=@filter.name%></h1>

<form method="get" action="/inventory">
  <input class ="btn_add_form_filter" type='submit' value="Clear Filter"/>
</form>
</div>

<br>
<br>
<table class = "inventory_table">
  <tr>
    <th>Game</th>
    <th>Genre</th>
    <th class = "filter">Publisher</th>
    <th>Stock</th>
    <th>Buy Price</th>
    <th>Sell Price</th>
    <th>Mark Up Rate</th>
  </tr>
  <% for game in @games %>
  <% if game.publisher_id == @filter.id %>
  <tr>
    <td><%= game.name%> </td>
    <td><%= game.genre%></td>
    <td><%= game.publisher.name%></td>
    <td><%= game.stock%> </td>
    <td><%= game.buy_price %></td>
    <td><%= game.sell_price %></td>
    <td><%= ((game.buy_price.to_f / game.sell_price.to_f)*100).round(2) %>%</td>
  </tr>
  <% end %>
  <% end %>

</table>
