<h1>Sales</h1>
<a class = "btn_add" href="/sales/new">Add New Sale</a>
<br>
<br>
<table>
  <tr>
    <th>Game</th>
    <th>Transaction Date</th>
    <th>Price</th>
    <th>Delete / Update</th>
  </tr>
  <% for sale in @sales %>
  <% game = sale.game %>
  <tr>
    <td><%= game.name if sale.game_id == game.id%></td>
    <td><%= sale.transaction_date%> </td>
    <td><%= game.sell_price if sale.game_id == game.id%> </td>
    <td id = "form_inline"><form method="post" action="/sales/<%= sale.id%>/delete">
      <input type='submit' value="Delete"/>
    </form>
    <form method="get" action="/sales/<%= sale.id %>/update">
      <input type='submit' value='Update'/>
    </form></td>
  </tr>
  <% end %>

</table>
